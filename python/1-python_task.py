#1
#a
import math
num = int(raw_input("Enter a number to be squared: "))
def square(num):
    y = math.pow(num,2)
    return y
print str(num) + " squared is equal to " + str(square(num))

return ^2
#b
3)
function general
//7

import mathdef calculate_average();
my_list = [1,2,3,4]
result = math.average(my_list)